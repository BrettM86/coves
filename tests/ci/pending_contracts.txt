# Collections the AppView ingests that do NOT have a pipeline contract yet.
#
# THIS FILE IS EMPTY AND MUST STAY THAT WAY. Task 15 retired the last four
# entries; every collection in internal/atproto/jetstream's consumer table now
# has a //coves:ingestion-contract marker in tests/e2e.
#
# docs/TEST_ARCHITECTURE.md §3.4a requires one ingestion contract per consumed
# collection: a record written straight to the PDS, indexed by the AppView's own
# consumers, observed on a serving endpoint. cmd/contract-manifest enforces it
# against that consumer table, and this file was the phase-4 burn-down that kept
# the check meaningful while the contracts were still being written — one domain
# per task, tasks 11-15.
#
# Format, one entry per line:
#
#   <collection>  # task <N>: why it is not contracted yet
#
# The task number is mandatory. A deferral nobody owns is how a burn-down
# becomes a permanent list, and this file has exactly one legal direction of
# travel: entries leave it. Adding a contract for a collection listed here makes
# the entry STALE and fails the check — delete the line in the same commit.
#
# TASK 16 flips cmd/contract-manifest to -allow-pending=false, now that the list
# is empty: after that, a new consumed collection fails the build immediately
# instead of being deferrable, and this file stops being a valid escape hatch.
# Task 20 VERIFIES the flag stayed flipped and this file stayed empty — it is
# part of the phase-6 enforcement sweep, not the flip itself.
#
# NOTE FOR TASK 16, WHICH DELETES tests/integration: there is no per-file carry
# list anywhere, and at least one file's coverage exists nowhere else.
# tests/integration/community_identifier_resolution_test.go (lines 163, 170,
# 176, 206) holds the only tests of the four malformed scoped-identifier forms —
# "!name" with no @, "!name@wrong.instance", "!@instance", and a bare name with
# no dots. Task 15 deleted block_handle_resolution_test.go, whose versions of
# those cases were false passes (no session, so the handler 401'd before
# resolution ran), on the strength of that file still existing. Relocate them
# into internal/core/communities before `rm -rf tests/integration`, or the
# coverage goes with it silently.
